{
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vitepress-plugin-lightbox": "^1.0.3"
  },
  "scripts": {
    "uconfig:manifest": "node scripts/generate-uconfig-manifest.mjs",
    "docs:dev": "npm run uconfig:manifest && vitepress dev",
    "docs:build": "npm run uconfig:manifest && vitepress build",
    "docs:preview": "vitepress preview"
  },
  "dependencies": {
    "medium-zoom": "^1.1.0",
    "prettier": "^3.5.3"
  }
}
